import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.indusrad.radixone',
  appName: 'radixone',
  webDir: 'dist',
  bundledWebRuntime: true
};

export default config;
